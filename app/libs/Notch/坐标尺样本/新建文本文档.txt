1.给对应设备安装该app

2.界面中有三条线（红、黄、绿），三条线分别显示如下数据，单位均为px。
  注：由于线有一定宽度，所以用线的上侧、右侧、左侧衡量。 

  红线：上侧距离顶部的距离

  黄线：右侧距离左边的距离

  绿线：左侧距离左边的距离

3.界面中还会获取到设备状态栏的高度（单位：px），
  这个数据可以和红线的数据进行一定校准（多数手机刘海的高度和状态栏高度是一致的）。

4.有几张样例供参考。